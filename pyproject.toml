[project]
name = "tripmind-ai"
version = "0.1.0"
description = "TripMind AI"
requires-python = ">=3.9"
dependencies = [
    "google-genai",
    "python-dotenv"
]

[tool.vercel]
entrypoint = "api.recommend:handler"